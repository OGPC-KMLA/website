[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Super Storm Bors."
authors = [{name = "Mateo A", email = "di4bmwjo9@mozmail.com"}]
dynamic = ["version", "description"]
